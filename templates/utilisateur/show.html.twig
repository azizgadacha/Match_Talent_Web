{% extends 'base1.html.twig' %}

{% block title %}Utilisateur{% endblock %}

{% block body %}


    <table class="table">
        <tbody>

        <h1><i class="fas fa-user fa-sm fa-fw mr-2 text-gray-400"></i>Informations utilisateur </h1>
        <br>
        {{ include('utilisateur/_delete_form.html.twig') }}
            <tr>
                <th>Id</th>
                <td>{{ utilisateur.id }}</td>
            </tr>

            <tr>
                <th>Username</th>
                <td>{{ utilisateur.username }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ utilisateur.address }}</td>
            </tr>
            <tr>
                <th>MotDePasse</th>
                <td>{{ utilisateur.motDePasse }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ utilisateur.email }}</td>
            </tr>
            <tr>
                <th>Contact</th>
                <td>{{ utilisateur.contact }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_utilisateur_index') }}">back to list</a>


{% endblock %}
