{% extends 'base1.html.twig' %}

{% block title %}ReponseReclamation index{% endblock %}

{% block body %}
    <h1>ReponseReclamation index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>IdReponse</th>
                <th>Reponse</th>
                <th>Date</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for reponse_reclamation in reponse_reclamations %}
            <tr>
                <td>{{ reponse_reclamation.idReponse }}</td>
                <td>{{ reponse_reclamation.reponse }}</td>
                <td>{{ reponse_reclamation.date ? reponse_reclamation.date|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('app_reponse_reclamation_show', {'idReponse': reponse_reclamation.idReponse}) }}">show</a>
                    <a href="{{ path('app_reponse_reclamation_edit', {'idReponse': reponse_reclamation.idReponse}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_reponse_reclamation_new') }}">Create new</a>
{% endblock %}
