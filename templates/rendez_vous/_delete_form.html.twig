<form method="post" action="{{ path('app_rendez_vous_delete', {'idRendezVous': rendez_vou.idRendezVous}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rendez_vou.idRendezVous) }}">
    <button class="btn">Delete</button>
</form>
