{% extends 'base.html.twig' %}

{% block title %}Quiz{% endblock %}

{% block body %}
   <div class="slider-area gray-bg position-relative">
<div class="single-slider d-flex align-items-center slider-height2">
<div class="container">
<div class="row">
<div class="col-xl-12">
<div class="hero-caption hero-caption2">
<h2>Quiz overview</h2>
</div>
</div>
</div>
</div>
</div>
</div>
<div class="container my-5">
    <h1 class="text-center">Quiz</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdQuiz</th>
                <td>{{ quiz.idQuiz }}</td>
            </tr>
            <tr>
                <th>NombreQuestions</th>
                <td>{{ quiz.nombreQuestions }}</td>
            </tr>
            <tr>
                <th>Barem</th>
                <td>{{ quiz.barem }}</td>
            </tr>
            <tr>
                <th>SujetQuiz</th>
                <td>{{ quiz.sujetQuiz }}</td>
            </tr>
        </tbody>
    </table>

    <div class="text-center">
        <a href="{{ path('app_quiz_index') }}" class="btn btn-primary">back to list</a>

        <a href="{{ path('app_quiz_edit', {'idQuiz': quiz.idQuiz}) }}" class="btn btn-warning">edit</a>

        {{ include('quiz/_delete_form.html.twig') }}

         <a href="{{ path('app_question_get_questions_by_id_quiz', {'idQuiz': quiz.idQuiz}) }}" class="btn btn-success">loop</a>
    </div>
</div>
{% endblock %}
